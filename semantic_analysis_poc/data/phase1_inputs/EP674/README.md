# EP674 Phase 1 Inputs

These files are small copied inputs from the completed Phase 1 transcription POC. They are local Phase 2 inputs, not regenerated outputs.

The original Phase 1 implementation remains in:

```text
../../../legacy_segment_poc/
```

Some copied metadata files contain historical absolute paths from the original runtime. Treat those paths as provenance only. Phase 2 should use the files in this directory and the paths listed in `input_manifest.json`.

The MP3, benchmark artifacts, deterministic topic outputs, and temporary files were intentionally not copied.
